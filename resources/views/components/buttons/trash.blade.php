<a href="{{ $url }}" class="btn btn-flat-{{ $color }} btn-labeled btn-labeled-start">
    <span class="btn-labeled-icon bg-{{ $color }} text-white">
        <i class="{{ $icon }}"></i>
    </span>
    {{ $title }}
</a>
